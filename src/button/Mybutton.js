const Mybutton = ({gBtnNm, color, sayAny, salute}) => {
  return (
    <div>
      {/* <button onClick={()=>sayAny(888)} className={color}>{gBtnNm}</button> */}
      {/* <button onClick={()=>salute(333)} className={color}>{gBtnNm}</button> */}
    </div> 
    
  )
}

export default Mybutton;